<div data-contact-target="content">
  <p class="paragraphe">Avec maximum un message par mois, nous vous informerons des nouveaux services proposés par le site, ou des appels à contributions pour nourrir cet outil ! Merci pour votre intérêt.</p>
  <%= simple_form_for [contact], url: create_newsletter_path, method: :post, class: "newsletter-form", data: {contact_target:"form"} do |f| %>
    <%= invisible_captcha %>
    <%= f.input :contact_email,
                    label: "E-mail",
                    required: true %>
    <%#= f.input :email, as: :hidden %>
    <div class="form-check mt10">
      <%= f.input :accept_cgu, label: false, error: false %>
      <%= f.label :accept_cgu do %>
        J'accepte la <%= link_to 'politique de confidentialité', politique_confidentialite_path %> *
      <% end %>
    </div>
    <% if error_cgu %>
      <p class="invalid-cgu">Vous devez valider la politique de confidentialité</p>
    <% end %>
    <%= f.input :contact_type,
                    input_html: { value: 'newsletter' },
                    as: :hidden %>
    <div class="btn-div">
      <%= f.button :submit, "Envoyer", class: "btn btn-purple", data: {action: "click->contact#create"} %>
    </div>
  <% end %>
</div>
